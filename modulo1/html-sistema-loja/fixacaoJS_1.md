```
` function calculaSalario(qtdeCarrosVendidos, valorTotalVendas) { `
`  let salario =                                                  `
`  let comissaoDoVendedor = qtdeCarrosVendidos * 100              `
`  let porcentagemComissao = 0.05 * valorTotalVendas              `
`  return comissaoDoVendedor + porcentagemComissao + salario      `
  
`}` 

```